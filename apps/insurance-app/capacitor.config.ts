import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'in.umun.insuranceApp',
    appName: 'insurance-app',
    webDir: "../../dist/apps/insurance-app",
    bundledWebRuntime: false
}

export default config;
